[tool.pytest.ini_options]
testpaths = ["backend/tests"]
asyncio_mode = "auto"
addopts = "-ra --strict-markers --strict-config"
markers = [
  "contract: PRD/test-spec contract checks for the Slack lunch bot MVP",
  "security: Slack/security/no-secret-leakage checks",
]
